{
  "name": "setup",
  "version": "1.0.0",
  "description": "Description",
  "main": "index.html",
  "scripts": {
    "build": "esbuild ./src --bundle --outfile=public/js/app.js --minify --watch",
    "start": "servor public index.html 8080 --reload --browse",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julio",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.12.9",
    "servor": "^4.0.2"
  },
  "dependencies": {}
}
